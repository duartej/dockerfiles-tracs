version: "3.2"
  
services:
    tracs-ubuntu:
        build: .
        image: duartej/tracs:1.0

    devcode_v1:
        image: duartej/tracs:1.0
        volumes:
            - /tmp/.X11-unix:/tmp/.X11-unix
            - type: bind
              source: @CODEDIR
              target: /home/tracs/tracs-code
        environment:
            - DISPLAY=unix${DISPLAY}
        depends_on:
            - "tracs-ubuntu"

